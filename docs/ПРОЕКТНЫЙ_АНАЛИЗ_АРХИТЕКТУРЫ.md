# АНАЛИЗ АРХИТЕКТУРЫ ПРОЕКТА ROLEPLAY IDENTITY
## Отчёт для настройки совместной работы фронтенда (React/Vite) в браузере и FiveM NUI

---

## 1. АРХИТЕКТУРА ПРОЕКТА

### 1.1 Структура монорепозитория

Проект организован как монорепозиторий с использованием **Nx** для управления зависимостями и сборкой:

```
RolePlayIdentity/
├── apps/
│   ├── client/                 # Личный кабинет (только браузер)
│   ├── mdtclient/             # MDT система (браузер + FiveM)
│   ├── server/                # Backend API
│   └── resources_fivem/       # FiveM ресурсы
├── libs/                      # Общие библиотеки
│   ├── shared-schema/         # Общие типы данных
│   ├── shared-types/          # TypeScript типы
│   └── shared-utils/          # Общие утилиты
├── scripts/                   # Скрипты сборки и деплоя
├── migrations/                # Миграции базы данных
└── supabase/                  # Конфигурация Supabase
```

### 1.2 Расположение фронтендов

**Личный кабинет (только браузер):**
- Путь: `apps/client/`
- Назначение: Веб-интерфейс для пользователей
- Технологии: React 18, Vite, TypeScript, Tailwind CSS

**MDT система (браузер + FiveM):**
- Путь: `apps/mdtclient/`
- Назначение: Система управления департаментами
- Поддерживает: Браузер и FiveM NUI
- Технологии: React 19, Vite, TypeScript

### 1.3 Backend расположение

**Основной сервер:**
- Путь: `apps/server/`
- Технологии: Node.js, Express, TypeScript
- База данных: PostgreSQL (Supabase)
- Порт: 5000 (основной)

### 1.4 Общие пакеты

**Корневые зависимости (package.json):**
```json
{
  "dependencies": {
    "@supabase/supabase-js": "^2.52.1",
    "@tanstack/react-query": "^5.83.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "zod": "^3.25.76",
    "tailwindcss": "^3.4.17"
  }
}
```

**Общие библиотеки:**
- `libs/shared-schema/` - схемы данных и валидация
- `libs/shared-types/` - TypeScript типы
- `libs/shared-utils/` - общие утилиты

---

## 2. КОНФИГУРАЦИЯ ФРОНТЕНДА

### 2.1 Vite конфигурация

**Личный кабинет (apps/client/vite.config.ts):**
```typescript
export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "src"),
      "@shared/schema": path.resolve(__dirname, "../../libs/shared-schema/src/index.ts"),
      "@roleplay-identity/shared-types": path.resolve(__dirname, "../../libs/shared-types/src"),
      "@roleplay-identity/shared-utils": path.resolve(__dirname, "../../libs/shared-utils/src"),
    },
  },
  build: {
    outDir: path.resolve(__dirname, "../dist/client"),
    emptyOutDir: true,
    rollupOptions: {
      output: {
        manualChunks: {
          'react-vendor': ['react', 'react-dom'],
          'ui-vendor': ['@radix-ui/react-dialog', 'lucide-react'],
          'charts-vendor': ['recharts', 'html2canvas', 'jspdf'],
          'forms-vendor': ['react-hook-form', '@hookform/resolvers', 'zod'],
          'i18n-vendor': ['i18next', 'react-i18next'],
        },
      },
    },
  },
  server: {
    port: parseInt(process.env.VITE_PORT || '3000'),
    proxy: {
      '/api': {
        target: 'http://127.0.0.1:5000',
        changeOrigin: true,
        secure: false,
      },
    },
  },
});
```

**MDT клиент (apps/mdtclient/vite.config.ts):**
```typescript
export default defineConfig(({ mode, command }) => {
  const env = loadEnv(mode, '.', '');
  const isNUI = command === 'build' && process.env.NUI === 'true';
  
  return {
    plugins: [react()],
    resolve: {
      alias: {
        "@": path.resolve(__dirname, "."),
      },
    },
    server: {
      port: 3001,
      proxy: {
        '/api': {
          target: 'http://127.0.0.1:5000',
          changeOrigin: true,
          secure: false,
        },
      },
    },
    build: {
      outDir: "dist",
      base: isNUI ? './' : '/',
      rollupOptions: {
        output: {
          // Для NUI убираем хеши в именах файлов
          entryFileNames: isNUI ? '[name].js' : '[name]-[hash].js',
          chunkFileNames: isNUI ? '[name].js' : '[name]-[hash].js',
          assetFileNames: isNUI ? '[name].[ext]' : '[name]-[hash].[ext]',
        }
      }
    },
  };
});
```

### 2.2 Основные скрипты

**Корневой package.json:**
```json
{
  "scripts": {
    "dev": "nx run-many --target=serve --projects=client,server --parallel",
    "dev:all": "nx run-many --target=serve --projects=client,mdtclient,server --parallel",
    "dev:client": "nx serve client",
    "dev:server": "nx serve server",
    "dev:mdtclient": "nx serve mdtclient",
    "build": "nx run-many --target=build --projects=client,server",
    "build:all": "nx run-many --target=build --projects=client,mdtclient,server",
    "build:fivem": "node scripts/build-fivem.js",
    "build:mdt:nui": "cd apps/mdtclient && NUI=true npm run build",
    "build:nui:quick": "node scripts/build-nui-quick.js",
    "build:deploy": "npm run build:all && npm run build:fivem"
  }
}
```

**Личный кабинет (apps/client/package.json):**
```json
{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "test": "jest"
  }
}
```

**MDT клиент (apps/mdtclient/package.json):**
```json
{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:nui": "vite build --base ./ && cp -r dist/* ../resources_fivem/mdt-system/ui/",
    "preview": "vite preview"
  }
}
```

### 2.3 Директории сборки

- **Личный кабинет:** `dist/client/` (относительно корня)
- **MDT браузер:** `apps/mdtclient/dist/`
- **MDT FiveM:** `apps/resources_fivem/mdt-system/ui/`

---

## 3. API И ВЗАИМОДЕЙСТВИЕ С BACKEND

### 3.1 Способы коммуникации

**Личный кабинет:**
```typescript
// apps/client/src/lib/supabase.ts
import { createClient } from '@supabase/supabase-js'

const supabaseUrl = import.meta.env.VITE_SUPABASE_URL
const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY

export const supabase = createClient(supabaseUrl, supabaseAnonKey)
```

**MDT клиент:**
```typescript
// apps/mdtclient/services/api.ts
const isNUI = typeof (window as any).GetParentResourceName === 'function';

export const API_BASE_URL = isNUI
  ? 'http://127.0.0.1:5000/api'  // Для FiveM NUI
  : '/api';                      // Для браузера (через proxy)
```

### 3.2 Различия между web и FiveM версиями

**Web версия:**
- Использует Vite proxy для API запросов
- Аутентификация через localStorage
- Полный доступ к браузерным API

**FiveM версия:**
- Прямые HTTP запросы к серверу
- Ограниченный доступ к браузерным API
- Специальная интеграция через NUI

### 3.3 Основные endpoints

**Аутентификация:**
- `POST /api/auth/login` - вход
- `POST /api/auth/register` - регистрация
- `GET /api/auth/me` - текущий пользователь

**Департаменты:**
- `GET /api/departments` - список департаментов
- `GET /api/departments/:id` - информация о департаменте

**Заявки:**
- `GET /api/applications` - список заявок
- `POST /api/applications` - создание заявки
- `GET /api/application-limits/:type` - проверка лимитов

**Отчёты:**
- `GET /api/reports` - список отчётов
- `POST /api/reports` - создание отчёта
- `GET /api/report-templates` - шаблоны отчётов

**Уведомления:**
- `GET /api/notifications` - список уведомлений
- `PUT /api/notifications/:id/read` - отметить как прочитанное

---

## 4. ИНТЕГРАЦИЯ С FIVEM

### 4.1 fxmanifest.lua

```lua
fx_version 'cerulean'
game 'gta5'

author 'RolePlayIdentity Team'
description 'MDT System for RolePlayIdentity'
version '1.0.0'

-- Client scripts
client_scripts {
    'client/main.lua'
}

-- Server scripts
server_scripts {
    'server/main.lua'
}

-- UI files
ui_page 'ui/index.html'

files {
    'ui/index.html',
    'ui/fivem-nui.js',
    'ui/**/*'
}

-- Dependencies
dependencies {
    'es_extended'
}

-- Export functions
exports {
    'openMDT',
    'closeMDT',
    'isMDTOpen'
}
```

### 4.2 Расположение UI

**Сборка для FiveM:**
- Исходники: `apps/mdtclient/`
- Сборка: `apps/mdtclient/dist/`
- Копирование: `apps/resources_fivem/mdt-system/ui/`

### 4.3 Подключение UI

**index.html для FiveM:**
```html
<!DOCTYPE html>
<html lang="en" class="dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>SnailyCAD v4 Concept</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script type="importmap">
  {
    "imports": {
      "react/": "https://esm.sh/react@^19.1.0/",
      "react": "https://esm.sh/react@^19.1.0",
      "@google/genai": "https://esm.sh/@google/genai@^1.11.0",
      "react-dom/": "https://esm.sh/react-dom@^19.1.0/",
      "lucide-react": "https://esm.sh/lucide-react@^0.525.0"
    }
  }
  </script>
  <script type="module" crossorigin src="./index.js"></script>
  <link rel="stylesheet" crossorigin href="./index.css">
</head>
<body class="font-mono text-secondary-200">
  <div id="root"></div>
  <script src="./fivem-nui.js"></script>
</body>
</html>
```

### 4.4 Lua скрипты

**client/main.lua:**
```lua
local isMDTOpen = false

local function openMDT()
    if isMDTOpen then return end
    
    if not hasPermission() then
        TriggerEvent('chat:addMessage', {
            color = {255, 0, 0},
            args = {"MDT", "У вас нет доступа к MDT системе"}
        })
        return
    end
    
    isMDTOpen = true
    SetNuiFocus(true, true)
    
    SendNUIMessage({
        type = 'openMDT',
        playerData = ESX.GetPlayerData()
    })
end

local function closeMDT()
    if not isMDTOpen then return end
    
    isMDTOpen = false
    SetNuiFocus(false, false)
    
    SendNUIMessage({
        type = 'closeMDT'
    })
end

RegisterCommand('mdt', function()
    openMDT()
end, false)

RegisterKeyMapping('mdt', 'Открыть MDT', 'keyboard', 'F6')
```

---

## 5. ПРОБЛЕМНЫЕ МОМЕНТЫ И ОПТИМИЗАЦИЯ

### 5.1 Текущие проблемы

**1. Дублирование кода:**
- Разные конфигурации Vite для web и NUI
- Отдельные API сервисы для разных клиентов
- Дублирование типов и интерфейсов

**2. Сложность сборки:**
- Отдельные скрипты для сборки FiveM версии
- Ручное копирование файлов
- Разные настройки для разных окружений

**3. Управление зависимостями:**
- Разные версии React (18 vs 19)
- Дублирование зависимостей в package.json
- Отсутствие единой системы управления состоянием

**4. API различия:**
- Разные способы аутентификации
- Разные базовые URL для API
- Отсутствие единого интерфейса

### 5.2 Рекомендации по оптимизации

**1. Унификация конфигурации:**
```typescript
// Общий vite.config.ts с условной логикой
export default defineConfig(({ mode, command }) => {
  const isNUI = command === 'build' && process.env.NUI === 'true';
  const isClient = process.env.APP_TYPE === 'client';
  
  return {
    plugins: [react()],
    build: {
      outDir: isNUI ? 'dist-nui' : 'dist',
      base: isNUI ? './' : '/',
      rollupOptions: {
        output: {
          entryFileNames: isNUI ? '[name].js' : '[name]-[hash].js',
        }
      }
    },
    server: {
      port: isClient ? 3000 : 3001,
      proxy: {
        '/api': {
          target: 'http://127.0.0.1:5000',
          changeOrigin: true,
        },
      },
    },
  };
});
```

**2. Единый API сервис:**
```typescript
// libs/shared-api/src/index.ts
export class ApiService {
  private baseUrl: string;
  
  constructor() {
    this.baseUrl = this.detectEnvironment();
  }
  
  private detectEnvironment(): string {
    if (typeof (window as any).GetParentResourceName === 'function') {
      return 'http://127.0.0.1:5000/api'; // FiveM
    }
    return '/api'; // Browser
  }
  
  // Единые методы для всех клиентов
}
```

**3. Общие компоненты:**
```typescript
// libs/shared-ui/src/index.ts
export { MDTInterface } from './components/MDTInterface';
export { AuthProvider } from './providers/AuthProvider';
export { useApi } from './hooks/useApi';
```

**4. Унифицированная сборка:**
```json
{
  "scripts": {
    "build:all": "nx run-many --target=build --projects=client,mdtclient,server",
    "build:fivem": "NUI=true nx build mdtclient && node scripts/copy-to-fivem.js",
    "dev:all": "nx run-many --target=serve --projects=client,mdtclient,server --parallel"
  }
}
```

### 5.3 Технические ограничения

**FiveM NUI:**
- Ограниченный доступ к браузерным API
- Необходимость относительных путей
- Отсутствие хешей в именах файлов
- Специальная обработка CORS

**Браузер:**
- Полный доступ к браузерным API
- Поддержка Service Workers
- Оптимизация через Vite HMR
- Современные возможности браузера

**Общие:**
- Единый backend API
- Общая база данных
- Совместимость с Supabase
- Поддержка JWT аутентификации

---

## 6. ЗАКЛЮЧЕНИЕ

### 6.1 Текущее состояние

Проект имеет хорошо структурированную архитектуру с разделением ответственности:
- **Личный кабинет** работает только в браузере
- **MDT система** работает в браузере и FiveM
- **Единый backend** обслуживает все клиенты

### 6.2 Приоритеты развития

1. **Унификация кода** - создание общих библиотек
2. **Упрощение сборки** - автоматизация процесса деплоя
3. **Улучшение DX** - единая система разработки
4. **Оптимизация производительности** - общие чанки и кэширование

### 6.3 Рекомендуемые действия

1. Создать `libs/shared-api` для единого API интерфейса
2. Унифицировать конфигурации Vite
3. Создать общие UI компоненты
4. Автоматизировать процесс сборки FiveM версии
5. Внедрить единую систему управления состоянием

Проект готов к масштабированию и имеет хорошую основу для дальнейшего развития. 